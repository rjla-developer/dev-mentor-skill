<!--
  Root CLAUDE.md. Hard cap: 150 lines, enforced by hooks/validate_claude_md.py.

  Include only what an agent or a new contributor cannot infer by reading the code.
  The test for every line: would removing it make someone pick the wrong file, command
  or limit? If not, delete it.

  Do not include: tutorials, changelogs, generic language rules, task notes that rot.
  Personal preferences go in CLAUDE.local.md (exempt from the cap, gitignored).

  Replace every {{PLACEHOLDER}} and delete the sections that do not apply.
-->

# {{PROJECT_NAME}}

{{ONE_SENTENCE_WHAT_THIS_IS}}

## Stack

- {{STACK_AND_VERSION}}
- Package manager: {{PACKAGE_MANAGER}}

## Commands

Run these exactly; do not substitute equivalents.

| Purpose | Command |
|---|---|
| Install | `{{INSTALL_COMMAND}}` |
| Run locally | `{{RUN_COMMAND}}` |
| Full test suite | `{{TEST_COMMAND}}` |
| Lint | `{{LINT_COMMAND}}` |
| Typecheck | `{{TYPECHECK_COMMAND}}` |
| Build | `{{BUILD_COMMAND}}` |

## Layout

{{WHERE_THINGS_LIVE_IN_3_TO_6_LINES}}

## Architectural decisions

Decisions that are already made. Changing one is a discussion, not a refactor.

- {{DECISION_1}} — because {{REASON_1}}
- {{DECISION_2}} — because {{REASON_2}}

## Conventions

- Branches: {{BRANCH_CONVENTION}}
- Commits: {{COMMIT_CONVENTION}}
- {{OTHER_CONVENTION}}

## Architectural rules

<!--
  From the stack's `architecture.rules` in the registry, plus anything this project
  decided for itself. Every line must be checkable by reading the code: "domain imports
  no SDK" is a rule, "keep it clean" is not. State WHO recommends it - the framework
  team's own guidance carries different weight from a preference.
-->

- {{ARCHITECTURE_RULE_1}}
- {{ARCHITECTURE_RULE_2}}
- {{ARCHITECTURE_RULE_3}}

Structure: {{FOLDER_STRATEGY}} — {{WHY_THIS_STRATEGY}}

## Known landmines

<!--
  Platform and library traps already paid for. Append here the moment one is found -
  during a task, not afterwards. This is the highest-value section in the file: it is the
  only part that cannot be re-derived by reading the code, because the code is what looks
  correct right up until the trap fires.
-->

- {{LANDMINE}} — {{WHAT_IT_BREAKS}} — {{WHAT_TO_DO_INSTEAD}}

## Behavioral rules

- **Think before coding.** State assumptions; when several readings are reasonable,
  present them rather than picking one silently.
- **Simplicity first.** No speculative generality, no abstraction with one implementation.
- **Surgical changes.** Change what the task requires; nothing adjacent.
- **Goal-directed execution.** Verifiable success criteria before starting.

On trivial work, shorten the flow and say that you did.

## Quality gate

- **Behavior changed** — logic, business rules, state, an API contract — **then a test is
  written or updated.** Moving a file or changing a colour does not need a new test.
- **Run `{{TEST_COMMAND}}` at the end of every task and report the real result.** Never
  claim done without running it. If it fails, fix it or say so.
- No coverage percentage targets. A test earns its place by being behavioral, specific,
  deterministic and non-duplicative.

## Testing rules

<!--
  From the stack's `testing.rules` and `what_not_to_test`. Stack-specific only - the
  quality gate above already covers when a test is required at all.
-->

- {{TEST_RULE_1}}
- {{TEST_RULE_2}}
- Not worth testing here: {{WHAT_NOT_TO_TEST}}

## Never

- {{NEVER_1}}
- {{NEVER_2}}
- Delete a file because it looks unused. Removal happens through a written plan that
  lists the evidence of non-use, and only after the suite passes. (`/mentor-clean` does
  this if it is installed; the requirement stands either way.)

## Sub-projects

<!-- Delete this section if the repository is not split by domain. -->

@{{PATH_TO_SUB_CLAUDE_MD_1}}
@{{PATH_TO_SUB_CLAUDE_MD_2}}
